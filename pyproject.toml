[project]
name = "sorteio"
version = "0.1.0"
description = "Sistema de sorteio de grupos para turmas"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.28.0",
    "pandas>=2.0.0",
]
